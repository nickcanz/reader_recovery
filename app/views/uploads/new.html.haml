%h3 New Upload

= form_for @upload, :html => { :multipart => true } do |f|
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :file
    = f.file_field :file
  .actions
    = f.submit
